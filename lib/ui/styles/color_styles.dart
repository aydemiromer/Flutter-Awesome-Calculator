import 'package:flutter/material.dart';

class UIColorStyles {
  static const Color PRIMARY_COLOR = Color(0xFFFF9800);
  static const Color DEEP_MODE = Colors.deepOrange;
  static Color background = Colors.deepOrange[100];
  static const Color TEXT_MODE = Color(0xFFFFFFFF);
  static const Color GREEN_MODE = Colors.green;
  static const Color RED_MODE = Colors.red;
}
